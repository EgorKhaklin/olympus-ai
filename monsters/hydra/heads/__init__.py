"""Heads of HYDRA — eight mortal, one immortal.

  Mortal heads (eight):
    cosmogony   — watches the constitution for drift
    pantheon    — watches the registry of named modules
    styx        — watches the oath chain for integrity
    journal     — watches the chronicle for silence
    oaths       — watches the cadence of new oaths
    lifecycle   — watches components in the lifecycle state machine
    substrate   — watches the filesystem layout
    apollo      — watches falsifiable-predicate coverage

  Immortal head (one):
    immortal    — watches the watchers (verifies all eight mortals
                  are still observing)
"""
